# hono-be

REST API with Hono.js

To install dependencies:

```bash
bun install
```

To generate database:

```bash
bun generate
```

To migrate database:

```bash
bun migrate
```

To run:

```bash
bun start
```
